
import numpy as np
# Constants

R = 1000 #Resistance (ohms)

C = 0.001 #Capacitance (farads)

V0 = 10 #Initial voltage (volts)

# Time points
t = np.linspace(0, 10, 1000)

# Charging model
V_charge = V0 * (1 - np.exp(-t/(R*C)))

# Discharging model
V_discharge = V0 * np.exp(-t/(R*C))

